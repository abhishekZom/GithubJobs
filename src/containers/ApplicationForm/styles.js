import variables from '../../utils/variables';

const styles = {
    container: {
        padding: '25px',
    },
    text: {
        fontSize: variables.FONT_SIZE_PRIMARY
    }
};

export default styles;
